# Инструкция по интеграции чата с Telegram

## ✅ Что уже готово

- ✅ Backend API полностью настроен
- ✅ База данных SQLite готова к работе
- ✅ Telegram бот интегрирован
- ✅ Frontend уже использует правильные API endpoints (не требует изменений)

## Архитектура решения

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│   КЛИЕНТ    │         │   RAILWAY    │         │  TELEGRAM   │
│  (Browser)  │         │   (Backend)  │         │    БОТ      │
└──────┬──────┘         └──────┬───────┘         └──────┬──────┘
       │                        │                         │
       │ POST /api/support/    │                         │
       │     sendMessage       │                         │
       ├───────────────────────>│                         │
       │                        │ Сохраняет в БД          │
       │                        │ messageFrom = 1         │
       │                        │                         │
       │                        │ Отправляет в Telegram   │
       │                        ├────────────────────────>│
       │                        │                         │ Оператор видит
       │                        │                         │ сообщение
       │                        │                         │
       │                        │ Оператор отвечает       │
       │                        │<────────────────────────┤
       │                        │                         │
       │                        │ Сохраняет в БД          │
       │                        │ messageFrom = 0         │
       │                        │                         │
       │ POST /api/support/     │                         │
       │     getMessages1       │                         │
       │<───────────────────────┤                         │
       │ {messages: [...]}     │                         │
```

## 📋 Пошаговая инструкция

### Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Meta Pay Support Bot`)
4. Введите username бота (должен заканчиваться на `bot`, например: `metapay_support_bot`)
5. **Скопируйте токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получение вашего Telegram Chat ID

**Вариант 1 (рекомендуется):**
1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему `/start`
3. Скопируйте ваш Chat ID (число вида `123456789`)

**Вариант 2:**
1. Напишите вашему боту любое сообщение
2. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
3. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID

### Шаг 3: Установка зависимостей локально (для тестирования)

```bash
npm install
```

### Шаг 4: Настройка переменных окружения в Railway

1. Откройте ваш проект в [Railway Dashboard](https://railway.app)
2. Перейдите в **Variables** (Переменные окружения)
3. Добавьте следующие переменные:

   | Переменная | Значение | Описание |
   |------------|----------|----------|
   | `TELEGRAM_BOT_TOKEN` | `123456789:ABCdef...` | Токен бота от BotFather |
   | `TELEGRAM_OPERATOR_CHAT_ID` | `123456789` | Ваш Chat ID |

4. Нажмите **Deploy** или Railway автоматически перезапустит проект

### Шаг 5: Деплой на Railway

1. Закоммитьте все изменения в Git:
   ```bash
   git add .
   git commit -m "Add Telegram integration"
   git push
   ```

2. Railway автоматически задеплоит проект

3. Проверьте логи в Railway Dashboard:
   - Должно быть: `Telegram бот запущен`
   - Если видите: `⚠️ Telegram бот не настроен` - проверьте переменные окружения

### Шаг 6: Тестирование

1. **Откройте сайт** на Railway (ваш домен)
2. **Откройте чат поддержки** (круглая кнопка внизу справа)
3. **Отправьте тестовое сообщение** (например: "Привет!")
4. **Проверьте Telegram** - должно прийти сообщение от бота с кнопкой "💬 Ответить"
5. **Ответьте в Telegram** одним из способов:
   - Нажмите кнопку "💬 Ответить" и отправьте сообщение
   - Или ответьте на сообщение клиента (reply)
   - Или используйте команду: `/reply <supportToken> <ваш ответ>`
6. **Проверьте сайт** - ответ должен появиться в чате через 1-2 секунды

## 🎯 Как отвечать клиентам в Telegram

### Способ 1: Кнопка "Ответить" (рекомендуется)
1. Нажмите кнопку "💬 Ответить" под сообщением клиента
2. Отправьте ваш ответ обычным текстом
3. Готово! Ответ отправлен клиенту

### Способ 2: Reply на сообщение
1. Ответьте на сообщение клиента (reply) в Telegram
2. Напишите ваш ответ
3. Готово!

### Способ 3: Команда /reply
```
/reply <supportToken> <ваш ответ>
```
Пример:
```
/reply abc123def456 Привет! Чем могу помочь?
```

## 📱 Полезные команды бота

- `/chats` - список всех активных чатов
- `/history <supportToken>` - история переписки с клиентом
- `/reply <supportToken> <сообщение>` - ответить клиенту

## 🔧 Устранение неполадок

### Бот не отвечает
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что `TELEGRAM_OPERATOR_CHAT_ID` правильный
- Проверьте логи в Railway Dashboard

### Сообщения не приходят в Telegram
- Убедитесь, что вы написали боту хотя бы одно сообщение (чтобы бот мог вам писать)
- Проверьте логи: должно быть `Сообщение отправлено оператору для токена: ...`

### Ответы не появляются на сайте
- Проверьте, что ответ сохранился в БД (логи: `Оператор ответил на чат ...`)
- Проверьте, что frontend делает polling (должен быть запрос к `/api/support/getMessages1` каждые 1.5 сек)

### База данных не создается
- Railway автоматически создаст файл `chat.db` при первом запуске
- Если проблемы, проверьте права на запись в Railway

## 📊 Структура базы данных

Таблица `messages`:
- `id` - уникальный ID сообщения
- `supportToken` - токен сессии клиента
- `message` - текст сообщения (NULL для изображений)
- `image` - base64 изображения (NULL для текста)
- `messageFrom` - 1 = от клиента, 0 = от оператора
- `createdAt` - время создания

## 🚀 Готово!

Теперь ваш сайт полностью интегрирован с Telegram. Клиенты пишут на сайте, а вы отвечаете в Telegram!

