# 🚀 Быстрый старт: Railway + Telegram

## Краткая инструкция

### 1. Создайте Telegram бота
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям и скопируйте токен

### 2. Разверните на Railway

1. **Создайте проект:**
   - Перейдите на https://railway.com/
   - Войдите через GitHub
   - "New Project" → "Deploy from GitHub repo"
   - Выберите репозиторий с `railway-backend`

2. **Добавьте PostgreSQL:**
   - В проекте: "+ New" → "Database" → "Add PostgreSQL"
   - `DATABASE_URL` добавится автоматически

3. **Настройте переменные:**
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
   ADMIN_CHAT_IDS=ваш_telegram_chat_id (опционально, для уведомлений)
   ```

4. **Деплой:**
   - Railway автоматически задеплоит проект
   - Скопируйте URL (например: `https://your-app.up.railway.app`)

### 3. Обновите frontend

```bash
node update-frontend-api.js https://your-app.up.railway.app
```

Или вручную замените `/api/support/` на `https://your-app.up.railway.app/api/support/` в файле `Meta Pay _ Meta_files/support.js.téléchargement`

### 4. Используйте

**Для оператора:**
1. Найдите вашего бота в Telegram
2. `/start` - начать работу
3. Когда клиент отправит сообщение, получите `support_token`
4. `/chat <support_token>` - подключиться к чату
5. Отправляйте сообщения - они появятся у клиента

**Для клиента:**
- Просто используйте чат на сайте как обычно
- Сообщения сохраняются в PostgreSQL на Railway
- Ответы оператора появляются автоматически

## Проверка работы

1. Откройте сайт с чатом
2. Отправьте тестовое сообщение
3. Проверьте Telegram - должно прийти уведомление (если настроен ADMIN_CHAT_IDS)
4. Подключитесь: `/chat <support_token>`
5. Отправьте ответ в Telegram
6. Проверьте сайт - сообщение должно появиться

## Структура проекта

```
railway-backend/
├── server.js          # Основной сервер с API и Telegram ботом
├── package.json       # Зависимости
├── railway.json       # Конфигурация Railway
└── README.md          # Подробная документация

update-frontend-api.js # Скрипт для обновления URL в frontend
SETUP_RAILWAY.md       # Подробная инструкция
```

## Архитектура

```
Клиент (браузер)
    ↓ POST /api/support/sendMessage
Railway Backend (Node.js + Express)
    ↓ Сохранение в PostgreSQL
    ↓ Уведомление в Telegram
Telegram Bot → Оператор

Оператор (Telegram)
    ↓ Отправка сообщения
Telegram Bot
    ↓ Сохранение в PostgreSQL
Railway Backend
    ↓ Polling каждые 1.5 сек
Клиент (браузер) ← POST /api/support/getMessages1
```

## Troubleshooting

**Бот не отвечает:**
- Проверьте `TELEGRAM_BOT_TOKEN` в Railway
- Проверьте логи в Railway Dashboard

**Сообщения не сохраняются:**
- Проверьте `DATABASE_URL` (должен быть автоматически)
- Проверьте логи на ошибки БД

**API не работает:**
- Проверьте URL: `https://your-app.up.railway.app/health`
- Должен вернуть `{"status":"ok"}`

## Стоимость

Railway предоставляет:
- **$5 бесплатно** каждый месяц
- PostgreSQL включен
- Автоматическое масштабирование

Для большинства проектов этого достаточно! 🎉

