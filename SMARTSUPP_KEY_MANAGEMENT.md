# Управление ключом Smartsupp через Telegram бота

## ✅ Реализовано

Теперь вы можете управлять ключом Smartsupp (`_smartsupp.key`) прямо через Telegram бота `@sumsub122_bot`!

## 📋 Доступные команды

### 1. Просмотр текущего ключа

```
/smartsupp_key
```

**Что делает:**
- Показывает текущий ключ Smartsupp
- Отображает маскированную версию (первые 8 и последние 4 символа)
- Показывает полный ключ для копирования

**Пример ответа:**
```
🔑 Текущий ключ Smartsupp:

`8aa708c7...33ca5`

📋 Полный ключ:
`8aa708c7d733a8fe8147c37aa98694304133cca5`

💡 Для изменения используйте:
`/smartsupp_set <новый_ключ>`
```

### 2. Установка нового ключа

```
/smartsupp_set <новый_ключ>
```

**Что делает:**
- Обновляет ключ в HTML файле (`Meta Pay _ Meta.html`)
- Сохраняет ключ в файл `smartsupp_key.json` для быстрого доступа
- Применяет изменения на сервере

**Пример использования:**
```
/smartsupp_set 8aa708c7d733a8fe8147c37aa98694304133cca5
```

**Пример ответа:**
```
✅ Ключ Smartsupp успешно обновлен!

🔑 Новый ключ:
`8aa708c7...33ca5`

📝 Изменения применены:
• HTML файл обновлен
• Ключ сохранен в файл

⚠️ Для применения изменений на сайте может потребоваться перезагрузка страницы
```

## 📁 Файлы

- `smartsuppKeyManager.js` - модуль для управления ключом
- `smartsupp_key.json` - файл с сохраненным ключом (создается автоматически)
- `Meta Pay _ Meta.html` - HTML файл, который обновляется при изменении ключа

## 🔄 Как это работает

1. **При первом запуске:**
   - Модуль загружает ключ из HTML файла
   - Сохраняет его в `smartsupp_key.json` для быстрого доступа

2. **При изменении ключа через бота:**
   - Ключ обновляется в HTML файле
   - Сохраняется в `smartsupp_key.json`
   - Изменения применяются сразу

3. **При просмотре ключа:**
   - Если ключ есть в памяти - показывается сразу
   - Если нет - загружается из файла или HTML

## ⚠️ Важно

- Минимальная длина ключа: 10 символов
- Изменения в HTML файле применяются сразу
- Для применения на сайте может потребоваться перезагрузка страницы
- Любой пользователь бота может использовать эти команды

## 🚀 Использование

1. Откройте Telegram бота `@sumsub122_bot`
2. Используйте команды:
   - `/smartsupp_key` - посмотреть текущий ключ
   - `/smartsupp_set <ключ>` - установить новый ключ

Готово! Теперь вся команда может управлять ключом Smartsupp через Telegram! 🎉

